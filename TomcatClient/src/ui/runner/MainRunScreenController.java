package ui.runner;

import javafx.fxml.FXML;

public class MainRunScreenController {
    @FXML
    private void initialize() {
        // No-op. SelectedProgram is already set before this screen loads.
        // Top table will auto-load; bottom table stays empty for now.
    }
}
